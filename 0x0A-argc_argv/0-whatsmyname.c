#include <stdio.h>

/**
 * main - function
 * @argc: number of argv
 * @argv: contrnt of command line argument
 *
 * Return: an int
 */

int main(int argc  __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
